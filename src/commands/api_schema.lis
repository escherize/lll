// GENERATED by scripts/gen_api_schema.py from the migrations' end state -
// do not edit. Regenerate with: mise run api-schema

/// The collection/field reference `lll api --schema` prints.
pub fn schema_reference() -> string {
  "
lll board API - collection and field reference

Generated from a board booted from pb/pb_migrations (the schema the
server enforces) by scripts/gen_api_schema.py; regenerate with
`mise run api-schema`. Never edit this output by hand.

Every collection is served under /api/collections/<name>/records with
list/view/create/update/delete, filtered by server filter
expressions: ?filter=(state='todo' && title~'login'), sorted with
?sort=-created. Rules name who may call each verb: (any) is every
authenticated member, (superuser only) is nobody else, anything else
is the rule itself. Auth is the member token the CLI stores
(lll login, or lll token create), as `Authorization: Bearer <token>`;
`lll api METHOD PATH` sends it for you.

## claims (base)

Rules: list @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= issue.team) view @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= issue.team) create @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= issue.team) update (superuser only) delete @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= issue.team)

| field | type | required | notes |
|-------|------|----------|-------|
| issue | relation | yes | relation to issues |
| member | relation | yes | relation to members |
| created | autodate |  | set on create |

## comments (base)

Rules: list @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= issue.team) view @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= issue.team) create @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= issue.team) update @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= issue.team) && (@request.auth.teams:length = 0 || @request.body.issue:isset = false) delete @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= issue.team)

| field | type | required | notes |
|-------|------|----------|-------|
| issue | relation | yes | relation to issues |
| author | relation |  | relation to members |
| body | text | yes |  |
| created | autodate |  | set on create |
| updated | autodate |  | set on create; refreshed on update |

## docs (base)

Rules: list @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team) view @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team) create @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team) update @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team) && (@request.auth.teams:length = 0 || @request.body.team:isset = false || @request.auth.teams.id ?= @request.body.team) delete @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team)

| field | type | required | notes |
|-------|------|----------|-------|
| slug | text | yes |  |
| title | text | yes |  |
| kind | text |  |  |
| body | text |  |  |
| issues | relation |  | maxSelect: 99; relation to issues |
| created | autodate |  | set on create |
| updated | autodate |  | set on create; refreshed on update |
| area | text |  |  |
| paths | text |  |  |
| team | relation | yes | relation to teams |
| confidence | select |  | values: suspected, confirmed, refuted |
| confidence_note | text |  |  |

## favorites (base)

Rules: list @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= issue.team) view @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= issue.team) create @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= issue.team) update @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= issue.team) && (@request.auth.teams:length = 0 || @request.body.issue:isset = false) delete @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= issue.team)

| field | type | required | notes |
|-------|------|----------|-------|
| issue | relation | yes | relation to issues |
| member | relation |  | relation to members |
| created | autodate |  | set on create |
| updated | autodate |  | set on create; refreshed on update |

## issues (base)

Rules: list @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team) view @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team) create @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team) update @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team) && (@request.auth.teams:length = 0 || @request.body.team:isset = false || @request.auth.teams.id ?= @request.body.team) delete @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team)

| field | type | required | notes |
|-------|------|----------|-------|
| team | relation | yes | relation to teams |
| number | number |  |  |
| title | text | yes |  |
| description | text |  |  |
| state | select | yes | values: backlog, todo, in-progress, in-review, done, cancelled |
| priority | number |  |  |
| sort | number |  |  |
| work_branch | text |  |  |
| work_host | text |  |  |
| work_path | text |  |  |
| assignee | relation |  | relation to members |
| project | relation |  | relation to projects |
| labels | relation |  | maxSelect: 999; relation to labels |
| created | autodate |  | set on create |
| updated | autodate |  | set on create; refreshed on update |
| emoji | text |  |  |
| blocked_by | relation |  | maxSelect: 999; relation to issues |
| attachments | file |  | maxSelect: 20 |
| creator | relation |  | relation to members |
| origin | json |  |  |
| refs | text |  |  |
| idempotency_key | text |  |  |
| idempotency_fingerprint | text |  |  |

## labels (base)

Rules: list @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team) view @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team) create @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team) update @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team) && (@request.auth.teams:length = 0 || @request.body.team:isset = false || @request.auth.teams.id ?= @request.body.team) delete @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team)

| field | type | required | notes |
|-------|------|----------|-------|
| name | text | yes |  |
| color | text |  |  |
| team | relation | yes | relation to teams |
| created | autodate |  | set on create |
| updated | autodate |  | set on create; refreshed on update |

## members (auth)

Rules: list @request.auth.id != \"\" view @request.auth.id != \"\" create @request.auth.id != \"\" && @request.auth.teams:length = 0 update @request.auth.id != \"\" && (@request.auth.teams:length = 0 || (id = @request.auth.id && @request.body.teams:isset = false)) delete (superuser only)

| field | type | required | notes |
|-------|------|----------|-------|
| name | text | yes |  |
| email | email | yes |  |
| created | autodate |  | set on create |
| updated | autodate |  | set on create; refreshed on update |
| password | password | yes |  |
| tokenKey | text | yes |  |
| emailVisibility | bool |  |  |
| verified | bool |  |  |
| kind | select |  | values: person, bot |
| owner | relation |  | relation to members |
| teams | relation |  | maxSelect: 999; relation to teams |

## projects (base)

Rules: list @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team) view @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team) create @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team) update @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team) && (@request.auth.teams:length = 0 || @request.body.team:isset = false || @request.auth.teams.id ?= @request.body.team) delete @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team)

| field | type | required | notes |
|-------|------|----------|-------|
| name | text | yes |  |
| description | text |  |  |
| status | select | yes | values: planned, started, paused, completed, cancelled |
| team | relation | yes | relation to teams |
| created | autodate |  | set on create |
| updated | autodate |  | set on create; refreshed on update |

## teams (base)

Rules: list @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= id) view @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= id) create @request.auth.id != \"\" && @request.auth.teams:length = 0 update @request.auth.id != \"\" && @request.auth.teams:length = 0 delete @request.auth.id != \"\" && @request.auth.teams:length = 0

| field | type | required | notes |
|-------|------|----------|-------|
| key | text | yes |  |
| name | text |  |  |
| archived | bool |  |  |
| created | autodate |  | set on create |
| updated | autodate |  | set on create; refreshed on update |
| accent | text |  |  |
| emoji | text |  |  |

## views (base)

Rules: list @request.auth.id != \"\" view @request.auth.id != \"\" create @request.auth.id != \"\" update @request.auth.id != \"\" delete @request.auth.id != \"\"

| field | type | required | notes |
|-------|------|----------|-------|
| name | text | yes |  |
| query | text | yes |  |
| member | relation |  | relation to members |
| created | autodate |  | set on create |
| updated | autodate |  | set on create; refreshed on update |

## webhooks (base)

Rules: list @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team) view @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team) create @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team) update @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team) && (@request.auth.teams:length = 0 || @request.body.team:isset = false || @request.auth.teams.id ?= @request.body.team) delete @request.auth.id != \"\" && (@request.auth.teams:length = 0 || @request.auth.teams.id ?= team)

| field | type | required | notes |
|-------|------|----------|-------|
| team | relation | yes | relation to teams |
| project | relation |  | relation to projects |
| url | text | yes |  |
| secret | text |  |  |
| created | autodate |  | set on create |
"
}
