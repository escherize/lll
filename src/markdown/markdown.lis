import "go:html/template"
import "go:github.com/escherize/lll/web"

/// Render user text through the private Go renderer, keeping its concrete
/// Goldmark/Chroma types out of generated Lisette bindings.
pub fn md_html(src: string) -> template.HTML {
  web.Markdown(src)
}
